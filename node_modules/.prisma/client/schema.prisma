generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Inventory {
  sku         String   @id
  quantity    Int
  location    String
  lastUpdated DateTime @default(now())
}
